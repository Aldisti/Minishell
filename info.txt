ls -a Utils | grep "it's $USERNAME" || pwd | grep '$(whoami)' && cat prova | grep 'a"a' < prova1

----
ls -a Utils
|
grep "it's $USERNAME"
||
pwd
|
grep '$(whoami)'
&&
cat prova
|
grep 'a"a' < prova1
----

MAIN
	set variables 1
		set env 2
	(while)
	readline
	parser
	espansioni
	redirection
	executor
		pipex
	exit collector

VARIABILE GLOBALE
	int	shell_errno


ls -a | (cat | grep .c) | (ls | echo)


gestire variabili env/exp nelle builtin 
	se il value == 0 la var non sta nell'env, ma nell'exp si
	se il value == ""(stringa vuota) la var sta in entrambi